Add CLI constraints support
+ Det password generator now accepts min/max length, quotas, mask with validation.
+ Legacy behavior stays default; README updated with example usage. Tests cover generator and CLI.
